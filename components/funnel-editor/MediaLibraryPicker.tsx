// Re-export of the shared media library so existing imports keep working.
// New code should import from '@/components/shared/MediaLibrary' directly.
export { default } from '@/components/shared/MediaLibrary';
export { uploadToMediaLibrary } from '@/components/shared/MediaLibrary';
